// File with localizations for the Pocker overlay in English

// Be sure that the pkt object exists
window.pkt = window.pkt || {};

// Add localization to object
window.pkt.r_i18n = {
  "add": "Saving tabs...",
  "success": "Complete",
  "error": "...but not all tabs were saved",
  "close": "close"
};

// Add this so we are able to inject the file. Basically what it does is
// to return undefined
void(0);
